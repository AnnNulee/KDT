도메인은 노드쓸때 DB 테이블정보를 model에서 넣어서 사용했듯이
java에서는 도메인을 쓴다.


원래는 서비스에 사용되는 데이터를 분리하는데 사용되었다.

트랜잭션 : 작업을 묶어놓은 것. 작업의 묶음.

작업을 지시하다보면 모델을 지정해야하고, 해당 모델을 비즈니스 로직에 담아서 사용하는 경우가 많은데,
그 비즈니스 로직과 모델을 구분해놓기 위해 만들어진게 domain