export default{ 

    methods:{
        //mixin js는 임포트 되면 mixin의 method를 마음대로 쓸 수있게 되는데, 이렇게 되면 컴포넌트 자체의 method와, mixin의 method를 구분하기 힘들어진다.
        //그래서 굳이굳이 $표시를 붙여서 mixin, 외부 method임을 나타낸다.

        
    }
}