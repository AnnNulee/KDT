

parent 인스턴스에는 child의 메소드나 변수들이 정의되지 않아서,
다운캐스팅을 해도 자식 클래스에서 추가된 메소드를 사용할 수 없다.

  Parent parent = new Child();
  여기서 parent 은 child 인스턴스의 주소값을 참조하는 변수.

  Child child = (Child) parent;
  여기서 parent의 클래스는 Child의 타입으로 재정의된다.

클래스의 타입 선언 (예: Parent parent1)
은 해당 클래스가 어떤 메소드와 변수를 가지고 있는지를 정의하는 것.

매개변수의 타입선언과는 다르다.
매개변수의 타입 선언은 특정 메소드에서 인수로 어떤 타입의 값을 받을지 정의하는 것이고,
그에 따라 메소드 내에서 사용할 수 있는 값이나 메소드가 결정됩니다.


오버라이딩은 자식 인스턴스 내에 오버라이딩이 정의 되었을때, 자식 인스턴스를 불러왔을때 적용된다.


